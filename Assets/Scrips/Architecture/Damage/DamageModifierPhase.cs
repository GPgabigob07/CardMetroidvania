namespace TicGame.Architecture
{
    public enum DamageModifierPhase
    {
        InstanceBuild = 0,
        PreTargetResolve = 10,
        PostTargetResolve = 20,
        PostRequestResolve = 30
    }
}

