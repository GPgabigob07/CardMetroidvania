namespace TicGame.Architecture
{
    public enum EnemyAIState
    {
        Idle = 0,
        Patrol = 10,
        Alert = 20,
        Chase = 30,
        Attack = 40,
        Stagger = 50,
        Dead = 60
    }
}

